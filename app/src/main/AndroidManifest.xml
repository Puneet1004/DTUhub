<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ashish.dtuhub">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".front">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".registrationpage" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".firstyear"
            android:label="@string/title_activity_firstyear"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".secondyear"
            android:label="@string/title_activity_secondyear"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".thirdyear"
            android:label="@string/title_activity_thirdyear"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".fourthyear"
            android:label="@string/title_activity_fourthyear"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>